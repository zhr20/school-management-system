# دليل الإعداد السريع

## خطوات الإعداد

### 1. إعداد قاعدة البيانات

1. شغّل WAMP Server
2. افتح phpMyAdmin من المتصفح: `http://localhost/phpmyadmin`
3. افتح ملف `api/database.sql`
4. انسخ محتوى الملف والصقه في phpMyAdmin
5. اضغط "تنفيذ" (Execute)

### 2. إعداد Backend

1. تأكد من أن مجلد المشروع موجود في:
   ```
   C:\wamp64\www\schoolMange\
   ```

2. إذا كان المشروع في مكان آخر، قم بنسخ مجلد `api` إلى:
   ```
   C:\wamp64\www\schoolMange\api\
   ```

3. تحقق من إعدادات قاعدة البيانات في `api/config.php`:
   - إذا كان كلمة مرور MySQL مختلفة عن الفارغة، قم بتحديثها

### 3. إعداد Frontend

1. افتح Terminal في مجلد المشروع
2. قم بتثبيت الحزم:
   ```bash
   npm install
   ```

3. قم بتشغيل الخادم:
   ```bash
   npm run dev
   ```

4. افتح المتصفح على: `http://localhost:3000`

### 4. اختبار النظام

1. تأكد من أن WAMP يعمل (الأيقونة خضراء)
2. تأكد من أن MySQL يعمل
3. افتح الصفحة الرئيسية وتحقق من ظهور الإحصائيات
4. جرب إضافة طالب جديد من صفحة الطلاب

## حل المشاكل الشائعة

### مشكلة: لا تظهر البيانات في الصفحة الرئيسية
- تحقق من أن قاعدة البيانات تم إنشاؤها بنجاح
- تحقق من أن ملف `api/config.php` يحتوي على الإعدادات الصحيحة
- افتح `http://localhost/schoolMange/api/dashboard.php` في المتصفح للتحقق من عمل API

### مشكلة: خطأ CORS
- تأكد من أن ملف `.htaccess` موجود في مجلد `api`
- تحقق من أن Apache يعمل في WAMP

### مشكلة: لا يمكن إضافة بيانات
- تحقق من أن MySQL يعمل
- تحقق من أن الجداول موجودة في قاعدة البيانات
- افتح ملف `api/config.php` وتحقق من إعدادات الاتصال

## ملاحظات مهمة

- تأكد من أن WAMP يعمل قبل تشغيل `npm run dev`
- إذا غيرت مسار المشروع، قم بتحديث `API_BASE_URL` في `nuxt.config.ts`
- قاعدة البيانات تحتوي على بيانات تجريبية للاختبار
